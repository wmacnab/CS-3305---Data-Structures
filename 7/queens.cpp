/*#include <iostream>
#include <stack>
using namespace std;

bool queens(int n)
{
    bool success = false;
    int row = 1, col = 1;
    stack<int> s;
    // Try placing row 1 queen in column 1...
    // Try placing row 3 queen in column 7...
    while (!success && !s.empty())
    {
        if () // There is a conflic
        // Pop elements off the stack until the stack becomes empty,
        // or the top of the stack is a choice that is not in column n.
        // If the stack is now not empty, then increase the column number
        // of the top choice by 1
        {

        }
        else if () // No conflict and stack size is n
        {
            success = true;
        }
        else
        // Push information onto the stack, indicating that the next choice is
        // to place a queen at row number s.size()+1, and column number 1
        {

        }
    }
}

int main()
{
    queens(5);
}*/